# TODO — 2026-04-23

Pick-up list after the Organization → Restaurant migration. The structural
work is done; everything below is polish + one real feature (credit packs).

## Current state

- All 7 migration phases complete and applied to prod (Supabase).
- Stripe keys are now live in `.env` (user set them up at end of prev session).
- Pre-existing Supabase backup covers rollback if anything below goes sideways.
- Stripe plugin is registered (`lib/auth.ts`) and mounts `/api/auth/stripe/webhook`.
- Only remaining TS error is unrelated: `OtpType` / `change-email` at
  `lib/auth.ts:145`. Item 6 below fixes it.

## Order of operations

Do them in this order — the earlier items are required-before-launch; later
ones are polish.

### 1. Changelog entry — ✅ DONE

Shipped as `v0.22.0` (2026-04-23) — "Plans, billing, and restaurant staff"
in `app/changelog/page.tsx`. Covers billing, trial, tiers, staff
invitations, read-only mode, activation picker, restaurant switcher, and
the shift of AI features to credit metering.

### 2. Credit pack purchase flow — ✅ DONE

Shipped: new `POST /api/billing/credit-pack/checkout` creates a one-time
Stripe Checkout session (mode=payment) stamped with
`metadata.kind = 'credit-pack-100'` + `metadata.organizationId`. The
`@better-auth/stripe` plugin's `onEvent` hook in `lib/auth.ts` catches
`checkout.session.completed`, verifies `payment_status === 'paid'`, and
calls `grantBonusCredits(orgId, 100, { type: 'purchase', reason:
'credit-pack-100' })`. Idempotent via sessionId lookup on
`CreditTransaction.metadata`. Button in `BillingPanel.tsx` now live; shows a
toast on `?creditPack=success|cancel` and scrubs the URL. Stripe customer is
auto-created if the org never subscribed before. Added changelog bullet to
v0.22.0.

**Remaining manual step**: create the $15 / 100-credit one-time price in
Stripe and uncomment `STRIPE_PRICE_CREDIT_PACK_100` at `.env:69`. Until
then, the route returns a clear 503.

<details>
<summary>Original brief</summary>

Button in `app/dashboard/billing/BillingPanel.tsx` currently disabled with
"coming soon." Needs:

- **`POST /api/billing/credit-pack/checkout`** — creates a one-time Stripe
  Checkout session (`mode: 'payment'`, single line item for
  `STRIPE_PRICE_CREDIT_PACK_100`). Pass `metadata.organizationId` so the
  webhook knows who to credit. Return the URL; client redirects.
- **Env var**: `STRIPE_PRICE_CREDIT_PACK_100` — user creates a one-time
  $15/100-credit price in Stripe dashboard, adds the id here.
- **Webhook handler**: add to `lib/auth.ts` under the stripe plugin's
  `onEvent` config. Listen for `checkout.session.completed`; if the session's
  `mode === 'payment'` and `metadata.organizationId` is present, call
  `grantBonusCredits(orgId, CREDIT_PACK.credits, { type: 'purchase', reason: 'credit-pack-100', metadata: { sessionId } })`.
  `grantBonusCredits` already exists in `lib/plans/credits.ts`.
- **UI**: enable the button in `BillingPanel.tsx`, wire it to POST the
  endpoint then `window.location.href = data.url`.

Tests: walk through in Stripe test mode. Verify the `CreditTransaction` row
has type=`purchase`, bucket=`bonus`, amount=100.
</details>

### 3. Hide account-level nav for staff — ✅ DONE

Sidebar `NAV` entries now carry an `orgOnly` flag; restaurant-scoped
viewers see only Menus and Staff. Server guard added at the top of
`/dashboard/settings`, `/dashboard/team`, and `/dashboard/billing` —
restaurant scope redirects to `/dashboard/menus` before any DB work.
Changelog note added under v0.22.0 `changed`.

<details>
<summary>Original brief</summary>

`getDashboardContext()` already returns `scope: 'org' | 'restaurant'`.
Restaurant-scoped staff (waiters/managers) should NOT see the Billing / Team
/ Settings links — server writes already block them, but the nav leaks.

- Pass `scope` from `app/dashboard/layout.tsx` into `DashboardSidebar`.
- In `components/dashboard/DashboardSidebar.tsx`, filter `NAV` when
  `scope === 'restaurant'` to exclude `/dashboard/team`, `/dashboard/billing`,
  `/dashboard/settings`. Keep `/dashboard/menus` and `/dashboard/staff`
  (staff can view the staff list — read-only gated inside the page).
- Also: restaurant-scoped users should probably never land on `/dashboard/settings`
  as a direct URL — add a `scope` check in that page that redirects to
  `/dashboard/menus` with a toast-less redirect.
</details>

### 4. Per-restaurant logo migration — ✅ DONE

Schema + code shipped. **Still needs**: apply the migration to Supabase.

- Prisma: added `logo String?` to `Restaurant`.
- Migration file: `prisma/migrations/20260423120000_restaurant_logo/migration.sql`
  — ALTER TABLE + idempotent backfill from `organization.logo` (only where
  `restaurant.logo IS NULL AND organization.logo IS NOT NULL`).
- Dual-write in `PATCH /api/organizations`: logo now writes to both
  better-auth (org) and the active Restaurant row.
- Reads updated to `restaurant.logo ?? org.logo`:
  - `app/m/[slug]/page.tsx` (header logo + JSON-LD `image`)
  - `app/dashboard/menus/[slug]/qr/page.tsx` (QR center logo)
  - `app/dashboard/layout.tsx` (sidebar)
  - `app/dashboard/settings/page.tsx` (settings form initial value)
- Bonus: `app/api/onboarding/create/route.ts` now stores the chosen logo on
  the newly-created restaurant too (was only going to the org row).
- Changelog bullet added under v0.22.0 `changed`.
- `Organization.logo` left in place — drop in a later cleanup once nothing
  reads the fallback.

<details>
<summary>Original brief</summary>

Deferred item from `notes/deferred-todos.md`. Logos are per-venue in a
multi-restaurant world but currently live on `Organization`.

- Schema: add `logo String?` to `Restaurant`.
- Migration: `ALTER TABLE restaurant ADD COLUMN logo TEXT;` plus a backfill
  `UPDATE restaurant r SET logo = (SELECT logo FROM organization WHERE id = r."organizationId")`.
  Use the manual-migration pattern we used in phase 3b-i.
- Dual-write: update `PATCH /api/organizations` so `logo` writes to BOTH
  `Organization.logo` (via better-auth for now) and `Restaurant.logo` (via
  Prisma, active restaurant only).
- Reads: update these files to read `restaurant.logo ?? org.logo`:
  - `app/m/[slug]/page.tsx` (lines ~195 and ~117 JSON-LD)
  - `app/dashboard/menus/[slug]/qr/page.tsx` (logo in QR panel)
  - `app/dashboard/layout.tsx` (sidebar)
  - `app/dashboard/settings/page.tsx` (settings form `logo` initial value)
- Settings form writes `logo` to Restaurant only, not org (going forward).
- Update `getDashboardContext` or the billing state if they pull logo.
- (Do NOT drop `Organization.logo` yet — do that in a later cleanup when
  you're confident nothing reads it.)
</details>

### 5. Trial banner (~45 min)

Global banner showing trial days remaining. Today only the billing page's
"Current plan" card shows this.

- Component: `components/dashboard/TrialBanner.tsx` (server component).
  Takes `trialEnd: Date | null` and `planName: string`. Renders a top-of-page
  bar with days remaining; turns red/urgent when `< 3 days`.
- Mount in `app/dashboard/layout.tsx` between `<ImpersonationBanner />` and
  `<ReadOnlyBanner />`. Only render when `subscription.status === 'trialing'`.
- Fetch trial state via `getBillingState(org.id)` in the layout (or add a
  thinner helper — the full billing state is heavier than needed).

### 6. Pre-existing OTP type error (~15 min)

`lib/auth.ts:145` — `sendVerificationOTP` is typed with `type: OtpType` but
the defined union doesn't include `"change-email"`, which better-auth
sometimes passes. Look at `lib/email-templates.ts` line 55:

```ts
type OtpType = 'sign-in' | 'email-verification' | 'forget-password'
```

Add `'change-email'` to the union, then handle it in the `otpEmailTemplate`
function's subject/actionText branches (treat it similar to
`email-verification`).

### 7. Stripe end-to-end test (~1 hr)

Real cards + real webhook. Don't touch code until you find something broken.

- Sign up as a new test user → onboarding → create a restaurant.
- Navigate to `/dashboard/billing` → click "Start 14-day trial" on Pro.
- Complete Stripe Checkout with `4242 4242 4242 4242` test card.
- Verify:
  - Webhook hits `/api/auth/stripe/webhook`.
  - `Subscription` row appears with `status: 'trialing'`, `plan: 'pro'`,
    `trialEnd` set ~14 days out.
  - `CreditTransaction` row grants 5 bonus credits (type=`grant`,
    reason=`trial-start`).
  - `Organization.bonusCreditsRemaining` is 5.
  - UI updates (billing page shows "Pro · trialing", credit widget shows 5).
- Create a menu to confirm credit spend lands in the monthly bucket
  (when plan activates post-trial).
- Then simulate a downgrade via Stripe Portal to a lower tier; verify
  `reconcileRestaurantActivation` kicks in and older restaurants flip to
  readOnly.

Anything that doesn't work, fix it then and there.

## Not doing today (still deferred)

See `notes/deferred-todos.md`:
- URL scoping `/dashboard/[restaurantSlug]/...` (skipped — not worth the churn)
- Onboarding post-signup redirect to scoped URL (depends on above)

## Files / modules touched most during the day

Expect edits in:
- `app/dashboard/billing/BillingPanel.tsx`, `page.tsx`
- `components/dashboard/DashboardSidebar.tsx`, `TrialBanner.tsx` (new)
- `lib/auth.ts` (webhook `onEvent`, OTP type fix)
- `lib/plans/credits.ts` (no change expected — just call from webhook)
- `prisma/schema.prisma` (logo field on Restaurant)
- `app/api/billing/credit-pack/checkout/route.ts` (new)
- `app/changelog/page.ts`
